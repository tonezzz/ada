[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: fast, hermetic tests (default)
    integration: tests that require external services, API keys, or non-hermetic dependencies
    slow: long-running tests
